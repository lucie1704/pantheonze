model Pastry {
  id          String   @id @default(uuid())
  name        String
  description String
  price       Float
  imageUrl    String?
  category    String
  ingredients String[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}